const sum = (num1, num2) => num1 + num2;
const substr = (num1, num2) => num1 - num2;
const division = (num1, num2) => num1 / num2;

// module.exports = sum;

module.exports = {
  sum,
  substr,
  division,
};
